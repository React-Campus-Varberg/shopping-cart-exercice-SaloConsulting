import React from 'react';

export default function Cart() {
  const cart = {
    padding: "1rem",
    margin: "1rem",
    borderRadius: "6px",
    backgroundColor: 'yellow',
  };

  return (
    <div style={cart}>
        This is the cart component:
    </div>
  )
}